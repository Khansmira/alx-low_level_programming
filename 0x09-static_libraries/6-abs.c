#include "main.h"

/**
 * _abs - this computes absolute value of an integer
 *
 * @n: an integer
 *
 * Return: absolute value of @n
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}

